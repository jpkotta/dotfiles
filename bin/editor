#!/bin/sh

EMACS=$(which xemacs)
EMACS_CLIENT=gnuclient

$EMACS "$@" 2>/dev/null

# # Start an emacs client
# # It will fail unless an emacs server is running, 
# #    in which case start the server.

# if [ -x "$EMACS" ] ; then
#     $EMACS_CLIENT "$@" 2>/dev/null \
# 	|| $EMACS "$@" 2>/dev/null
# fi